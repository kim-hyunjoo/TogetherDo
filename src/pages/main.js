import React from "react";
import Calendar from "../components/Calendar";

const Main = () => (
    <div>
        <div className="calendar">
            <Calendar />
        </div>
    </div>
);

export default Main;
